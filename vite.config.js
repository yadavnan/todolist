import { defineConfig } from 'vite';

export default defineConfig({
  // This config is optional for basic setups
  server: {
    open: true,  // Automatically open the app in browser on startup
  },
});
